

spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
  application:
    name: boxservice

eureka:
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/
    register-with-eureka: true
    fetch-registry: true